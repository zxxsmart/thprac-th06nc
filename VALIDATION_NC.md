# 红魔乡新典适配验证记录

本地验证日期：2026-09-11。上游基线为 `55e6ed1336621e3099e7df1138acc360cf0500fa`，本地分支为 `feature/th06nc`。对象是 Steam th06nc 1.03，EXE SHA-256：`07850c8c6e469c0e82c13423e6d0d096a88d693455bdacacbb44c0aa3bcce473`。

## Rank 修正（当前版本）

默认不覆盖游戏初始化的 Rank。UI 读取本机 EXE 的原生难度表（RVA `0x3084e0`），Easy／Normal／Hard／Lunatic／Extra 分别为 20／32／32／32／18，关闭“自定义 Rank”时显示禁用的数值栏。开启后允许 0–99，仅在练习初始化时写入；暂停调整在重开后生效。

原版 TH06 的 Rank Lock 文案及逻辑保持原样。新典使用独立的“自定义 Rank”本地化条目和设置标志。删除旧 v5 录像兼容的最大资源恢复、出生无敌例外；当前只读取 v7 参数，运行中事件仅记录辅助开关和启用状态，Rank 保存在起始参数中。

本轮完整构建通过。运行检查覆盖五个难度的原生值、自定义 0／12／99 的保持、未重开时仍使用当前值、关闭自定义后重开恢复 Easy 20，以及重新开启后重开应用 12。另从原生菜单保存并播放 Rank 12 的 v7 录像，当前预设改为 99、关闭自定义后，回放仍正确恢复 12 并正常结束。证据见 `docs/th06nc/rank-validation.json`，Extra 默认值 UI 截图见 `rank-native-extra.jpg`。

通过 Steam 正常启动完成本轮验证；独立目录中的 EXE 副本仍被本机安全软件拦截。下方旧版本的兼容说明与未完成项属于历史记录，不代表当前支持旧开发版录像。

复测脚本：`tools/check_nc_rank.py PID`，需要指定已进入自定义练习的测试进程，会重开该练习并改变其参数。

## 实现与用户流程

现有 32 位 thprac 启动器保留原有游戏列表、配置和旧游戏模块，增加新典的游戏识别及 Steam 实例。随包的 64 位桥接程序加载同一源码树构建的新典模块。新典 UI 使用上游 ImGui、字体、控件与本地化文本，练习菜单、暂停菜单、70 个 Boss 选段补丁和 40 个道中入口均从 `thprac_th06.cpp` 迁移。

实际操作验证了：从打包后的 `thprac.exe` 启动用户安装目录的 Steam 游戏，选择 Practice Start、难度、机体、单面，出现 thprac 练习选项；选择关底 Boss 并按 Z 后直接开局。暂停后可继续、退出、重开和调整选项。原版 th06 1.02h 也通过现有启动器加载，显示了原版 thprac 高级选项。

自定义选段设置的是原生关卡练习标志（Practice=1、Spell Practice=0），并修补当前关卡 ECL 和时间轴。它不会调用游戏内置的单符练习入口。实测一面 Night Bird 超时后进入后续非符，保持同一关卡练习状态。

## 本轮修正与验证

- 保留用户已确认正常的鼠标操作、Extra 三张新增符卡与当前资源锁定。暂停 UI 对齐居中版面；跳段时隐藏关卡标题并跳过出生无敌，从关卡起点开始则保留。
- 三面恢复原有七个道中入口。后半 #3 使用原映射时间 4994，新典追加波次从 5174 开始，随该段原生时间轴继续执行；删除单独的后半 #4 入口及对应分支。
- Backspace 只保留 F1–F7 开关。删除快捷菜单的重开、保存按钮，以及运行中即时导出、自动寻找空槽和恢复录像管理器状态的代码。暂停菜单保留原版的重开功能。
- 暂停退出改为原生结果状态 7。结果初始化仅对本模块的非回放练习生效：以状态 9 加载原生界面而不登记高分，再进入状态 10 的保存询问。正常练习通关也使用同一原生结果初始化路径。选择“是”、槽位和名字后由游戏保存 `.rpy`，写文件挂钩附加练习参数。

| 本轮检查 | 结果 |
| --- | --- |
| 完整构建 | 原有 x86 ReleaseLLVM 启动器、新增 x64 模块与桥接程序成功构建；测试进程关闭后完成便携打包 |
| ECL 审计 | 73 个 Boss 选段和三面后半 #3，分别执行 2 种对话 × 4 种机体，共 592 次，失败 0 |
| 资源与开局状态 | 关卡起点保留标题和出生无敌；Boss、道中和指定帧入口跳过；锁定当前残机、Bomb、火力，实际 Bomb 与 Miss 后保持数值，解除后恢复原生扣减，重新开启重新捕获；0 残机锁定仍正常重生 |
| 新增 Extra | 71、72、73 分别进入原生攻击子程序 104、109、115，血量 3900、4000、6000；第一张至第二张、第二张至第三张的原生阶段切换通过 |
| 暂停退出保存 | 实测显示“要保存此次 Replay 吗？”（状态 10），确认后进入原生槽位（12）、名字（13）；最终确认前导出计数 0，确认后为 1 |
| 保存文件 | 新录像 176 字节，参数文件 128 字节、格式 v6、1 个开关事件、结束点 148；参数中的原生录像摘要与实际文件一致 |

资源、开局与 Extra 检查在本轮前半完成，用户随后确认鼠标、新符卡、资源正常。机器记录见 `docs/th06nc/revision-2.json`，其中旧的 `stage3-added-wave` 记录来自合并前的临时 10308 入口，**不代表最终后半 #3 的运行复测**。合并后的入口通过 ECL 审计；本次不重复宣称已完成所有旧版运行检查。

最后一次重启测试时，Windows 将独立测试副本的游戏 EXE 拦截并移除；从 Steam 原文件重新复制后仍返回“包含病毒或潜在的垃圾软件”。因此，最终版本的自然通关询问、选择“不保存”、v6 回放状态比对，以及合并后的三面新增波次尚未完成运行复测。自然通关入口已按本机反编译代码核对为同一结果状态 7；这属于代码核对，不替代运行验证。

## 首版已有的运行检查

| 检查 | 覆盖与结果 |
| --- | --- |
| 构建 | 原有 x86 ReleaseLLVM / ClangCL 启动器和新增 x64 Release 模块均成功构建，便携目录包含桥接程序、模块与 FreeType 依赖 |
| ECL 静态执行审计 | 执行真实补丁代码，70 个 Boss 选段 × 2 个对话设置 × 4 个机体，共 560 次，映射及脚本校验失败数为 0 |
| Boss 入口运行检查 | 70/70，通常使用 Hard 灵梦 A，Extra 使用 Extra 难度；检查脚本应用成功、目标阶段活动、Practice=1、Spell Practice=0 |
| 道中入口运行检查 | 40/40，按上游 THStageWarp 分段；在观察窗口内检查敌人活动，允许波次之间的空档 |
| 四面假机体 | 4 种假机体 × 7 个关底阶段，共 28 次；实际玩家机体保持不变，ECL 按假机体分支运行 |
| 六面咲夜第二阶段 | 4 种机体分别确认 750、1000、1100、1100 的初始血量 |
| Extra 最后一符 | 正常与发狂两个阶段，观察攻击循环等待值，确认两种阶段分别使用不同的攻击周期 |
| 开局时间锁定 | 一、二、四、五面道中 Boss，以及一面关底 Boss，5 个入口均进入正常攻击，未卡在登场状态 |
| 辅助功能 | 无限残机、Bomb、火力；无敌；自动 Bomb 实际触发且检查时 Miss=0；Rank 锁定；自定义资源；Boss BGM 路径 |
| 暂停与调整 | 暂停时 Boss 帧数保持不变；实际 UI 切换为原版练习并按 R 重开，恢复 2 残机、3 Bomb、0 火力、原版 Rank 和关卡起点 |
| 普通模式隔离 | Start、Extra Start、内置 Spell Practice 分别检查原生模式标志和 60 FPS，均不应用预设练习资源或 ECL 选段；普通 Start 暂停保持游戏原生菜单 |
| 练习录像 | 从原生 Replay 菜单播放；最终协议 v5 检查自定义资源、Rank 和运行中时间锁定开关，5 个每 60 帧采样点的敌人状态摘要、随机数、残机、Bomb、擦弹、分数均一致；保存点结束后返回菜单、恢复 60 FPS |

首版较早的长录像检查还覆盖了 22 个每 60 帧的采样点，未发现差异。首版使用 v5；本轮新录像使用 v6，同时保留 v5 读取及旧资源锁定/开局行为，以维持旧录像语义。必须同时保留 `.rpy` 与同名 `.rpy.thprac-nc` 文件。此前的 v5 比对结果不能当作本轮 v6 回放的运行验证。

## 复现与证据

源代码仓库中的 `docs/th06nc/validation.json` 保存首版结果，`revision-2.json` 保存本轮结果与待复测项。`docs/th06nc/` 下保存启动器、选段菜单、暂停调整菜单以及原生保存询问截图。便携程序包仅包含本说明，不包含测试游戏、录像或游戏资源。

- `tools/audit_nc_ecl.cpp`：在 x64 构建环境下执行真实 ECL 修补代码，输入参数为本地提取的 `ecldata1.ecl` 至 `ecldata7.ecl` 所在目录。
- `tools/smoke_nc_recipes.py PID`：对已通过 UI 进入自定义练习的测试进程执行 73 个选段；加 `--portions` 执行 40 个道中入口。
- `tools/check_nc_branches.py PID`：特殊机体、阶段、时间锁定和辅助功能检查。
- `tools/check_nc_features.py PID 游戏目录`：资源与锁时检查，随后打开原生录像保存询问，由操作者选择槽位和名字保存。
- `tools/check_nc_revision.py PID`：当前资源锁定、开局状态、三面后半 #3 与新增 Extra 阶段检查；会在测试副本中模拟 Miss 和降低 Boss 血量。
- `tools/check_nc_exit.py PID [虚拟键码...]`：操作并读取原生录像询问、槽位与名字状态，保存当前界面截图；不直接调用录像导出函数。
- 设置环境变量 `TH06NC_PRACTICE_TRACE=1` 后启动测试游戏，可输出每 60 帧的状态摘要。正常运行默认不输出此诊断文件。

这些脚本会改变指定测试进程的练习参数、重开当前练习，并可能写入该游戏目录的录像；应使用独立测试副本。生成 ECL 映射需要本地解析数据，不需要重新生成映射即可构建已有适配。

## 验证边界

70 个入口的检查是攻击阶段与关卡流程的运行检查，不等于遍历所有难度、机体、对话、指定帧和后续阶段的全部组合，也不等于逐像素比较原版与新典的弹幕。新典本身的弹幕和画面变化保持新典行为。F12 提供帧率调整；原版 th06 专用的渲染、窗口和兼容性补丁没有机械移植到 D3D11 新典。

当前支持上述准确版本的 EXE 和七个关卡脚本。启动时核对 EXE 完整哈希，修补前核对 ECL 大小及摘要，版本不匹配会拒绝应用，后续 Steam 更新需重新适配。练习模块不覆盖安装目录的游戏二进制或游戏资源。
